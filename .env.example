# Copy to .env.local and fill values locally.
# Never commit real Contentful IDs or tokens.

CONTENTFUL_SPACE_ID=
CONTENTFUL_ENVIRONMENT_ID=dev
CONTENTFUL_DEFAULT_LOCALE=

CONTENTFUL_MANAGEMENT_TOKEN=
CONTENTFUL_DELIVERY_TOKEN=
CONTENTFUL_PREVIEW_TOKEN=

# Verification uses a freshly recreated dev environment.
# No persistent verification environment is used.

# Optional governed snapshot selector.
#
# Leave blank during export so export-model.mjs generates:
# contentful-model.dev.v1.<YYYYMMDDTHHMMSSZ>.json
#
# For later verification/import, set only an explicitly approved
# governed snapshot filename.
CONTENTFUL_MODEL_SNAPSHOT=
